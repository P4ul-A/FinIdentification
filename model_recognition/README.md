# Private fin-recognition models

This folder is included in GitHub downloads, but all `*.pt` weights are
excluded by the repository `.gitignore`.

Copy the separately supplied fin detector here using:

```text
model_<name>.pt
```

The desktop app discovers matching files when it starts.
